# Agent System Design

지난 대화에서 정의한 mshuttle 운영 자동화 Agent 시스템의 전체 설계.

## 개요

**목표:** 운영팀을 자동화된 AI Agent로 대체
**상황:** 
- DB에는 데이터가 있지만 구조적 개선 필요
- 운영팀이 업무에 관심 없음 → 자동화 필수
- 현재 자동화되지 않은 운영 프로세스 다수 존재

**전략:** Agent 시스템으로 모든 의사결정 자동화

---

## 7개 Core Agent

### 1️⃣ **RouteManagerAgent** (경로 관리자)
**책임:** 신청자 데이터 분석 → 클러스터링 → 경로 자동 생성

| 항목 | 내용 |
|------|------|
| **입력** | 알림받기 신청 데이터 + 기간 + 지역 |
| **의사결정** | 클러스터 분석 → 경로 생성 가능한 그룹 판단 |
| **산출물** | 경로 객체 + 10개 메타데이터 |
| **의존성** | 신청자 데이터 필수 |
| **실행주기** | 일일 (또는 수동 트리거) |

**상세 프로세스:**
```
1. 신청자 데이터 수집 (기간, 지역, 시간대별 필터)
2. 클러스터링 분석 (zoom 레벨 자동 결정)
3. 경로 가능한 그룹 판단 (최소 인원 등 기준 적용)
4. 6단계 시스템 프로세스 실행
   - Point 생성 (정류장 후보)
   - LineString 생성 (Tmap 길찾기)
   - 정류장 확정
   - 자동 생성
   - 메타데이터 입력
5. 경로 생성 완료 → DispatcherAgent에 전달
```

**결정 규칙:**
- 클러스터 최소 인원 기준: [설정 필요]
- 클러스터 반경: [설정 필요]
- 탑승지-하차지 거리: [설정 필요]

---

### 2️⃣ **DispatcherAgent** (배차 담당자)
**책임:** 생성된 경로에 기사 자동 배정

| 항목 | 내용 |
|------|------|
| **입력** | 경로 객체 + 기사풀 데이터 |
| **의사결정** | 최적 기사 선택 |
| **산출물** | 배차 완료 + 기사 연락처 |
| **의존성** | RouteManagerAgent의 경로 필수 |
| **실행주기** | 일일 (경로 생성 후) |

**상세 프로세스:**
```
1. 경로 정보 수신
2. 기사풀 현황 확인
   - 사용 가능한 기사 목록
   - 각 기사의 연계 운행 시간 확인
3. 최적 기사 선택
   - 공차(deadheading) 최소화
   - 연계 운행 시간대 맞춤
   - 기사 선호도 고려 [설정 필요]
4. 배차 확정
5. MarketingAgent & 기사에게 전달
```

**결정 규칙:**
- 공차 최소화 공식: [설정 필요]
- 기사 선택 우선순위: [설정 필요]
- 기사 부족 시 처리: [설정 필요]

---

### 3️⃣ **MarketingAgent** (마케팅 담당자)
**책임:** 배차 완료된 경로의 신청자 자동 모집

| 항목 | 내용 |
|------|------|
| **입력** | 배차 완료된 경로 + 신청자 목록 |
| **의사결정** | 연락 대상 선정 + 연락 시점 |
| **산출물** | 탑승자 확정 명단 |
| **의존성** | DispatcherAgent의 배차 필수 |
| **실행주기** | 배차 직후 |

**상세 프로세스:**
```
1. 배차 완료 경로 정보 수신
2. 신청자 목록 확인
3. 연락 전략 결정
   - 우선순위 정렬
   - 연락 채널 선택 (전화/SMS)
   - 연락 시점 결정
4. 자동 연락 실행 (SMS API 또는 통합 시스템)
5. 응답 추적
6. 목표 인원 달성 시 ControlAgent에 전달
```

**결정 규칙:**
- 목표 인원: [설정 필요]
- 연락 채널: SMS/전화/기타
- 재연락 규칙: [설정 필요]

---

### 4️⃣ **ControlAgent** (관제사)
**책임:** 운행 중인 경로 실시간 모니터링

| 항목 | 내용 |
|------|------|
| **입력** | 운행 중인 경로 + 드라이버 앱 데이터 |
| **의사결정** | 이상 상황 감지 및 대응 판단 |
| **산출물** | 운행 로그 + 이상 신고 |
| **의존성** | 기사의 앱 데이터 필수 |
| **실행주기** | 실시간 (운행 중) |

**모니터링 항목:**
- 실시간 위치 추적
- 시간 지키는지 확인
- 탑승/하차 확인 (앱 입력)
- 운행 거리 확인
- 이상 상황 감지 (지연, 경로 이탈 등)

---

### 5️⃣ **SettlementAgent** (정산 담당자) ⭐ **우선 자동화**
**책임:** 월 1회 재정 정산 자동 계산

| 항목 | 내용 |
|------|------|
| **입력** | 운행 완료 데이터 (월별) + 계약 정보 + 이슈/중단 기록 |
| **의사결정** | 수학적 계산만 (의사결정 없음) |
| **산출물** | 정산 보고서 + 총 운행비 (기사 지급액) |
| **의존성** | ControlAgent의 운행 데이터 필수 |
| **실행주기** | 매월 자동 (월 5일 예정) |

**핵심 계산 공식:**

```
[1단계] 월계약 운행비
  defaultAmount = payAmount(계약금액) + addAmount(조정금액)

[2단계] 일수 계산
  monthDayCnt   = 정산 기간 내 총 일수
  exceptDayCnt  = monthDayCnt - 캘린더운행일수 (제외일)
  pauseDay      = 경로 중단 기간 일수
  removeDay     = 이슈 미운행 일수
  notRunnDayCnt = pauseDay + removeDay
  runnDayCnt    = monthDayCnt - notRunnDayCnt - exceptDayCnt (실 운행일)

[3단계] 미운행비용
  notRunnAmount = floor(-1 × defaultAmount × (exceptDayCnt + notRunnDayCnt) / monthDayCnt)

[4단계] 이슈 금액
  issAddAmount    = sum(이슈 추가비, sign=1)
  issRemoveAmount = -sum(이슈 차감비, sign=0)

[5단계] 공급가액 + 부가세
  supplyAmount = defaultAmount + issAddAmount + issRemoveAmount + notRunnAmount
  vatAmount    = 과세(a): round(supplyAmount × 0.1) / 면세(b): 0

[6단계] 최종 지급액
  totalRunnAmount = supplyAmount + vatAmount + etcAmount + refAmount(소개비)
```

**정산 기간 결정 (setlStartDay 기준):**
- 10일 이하 → 당월 시작 (예: 3/5 ~ 4/4)
- 10일 초과 → 전월 시작 (예: 2/15 ~ 3/14)

**특수 케이스 처리:**

| 케이스 | 처리 방식 |
|--------|----------|
| 운행중단 (RtPause) | `pauseDay`에 반영 → 미운행비용 차감 |
| 이슈 미운행 (DrIss) | `removeDay`에 반영 → 미운행비용 차감 |
| 이슈 금액 조정 (DrIssAmount) | sign=1 추가 / sign=0 차감 |
| 조정금액 (DrPayAdjust) | `defaultAmount`에 직접 반영 |
| 소개비 (DrRef) | 최종 금액에 합산 |
| 신규 경로 | runnStatus="신규", 기간 내 시작일부터 계산 |
| 폐지 경로 | runnStatus="폐지", 기간 내 종료일까지 계산 |

**정산 상태 흐름:**
```
미정산 → [정산 실행] → 정산완료 → [이체] → 이체완료(잠금)
                        ↓
                   [정산 취소] → 미정산
```

**DB 테이블 매핑:**

| DB 테이블 | 키 칼럼 | 용도 |
|-----------|---------|------|
| `dr_pay` | amount, setl_day_cd, setl_start_day | 계약금액, 정산 기간 |
| `dr_setl` | vat_cd, transfer_yn, total_runn_amount | 정산 결과 저장 |
| `cald` | day, runn_cd, is_holiday | 운행 캘린더 |
| `rt_pause` | start_day, end_day, pause_cd | 경로 중단 기간 |
| `dr_iss` | dispatch_id, start_day, end_day, iss_cd | 이슈 미운행 |
| `dr_iss_amount` | sign, amount, iss_amount_cd | 이슈 금액 조정 |
| `dr_pay_adjust` | amount, setl_start/end_month | 조정금액 (기간 한정) |
| `dr_ref` | amount, ref_dr_id | 소개비 |

**정보 수집 완료 항목:**
- [x] 정산 계산 공식 (코드 분석 완료)
- [x] 데이터 엔티티 구조
- [x] 특수 케이스 처리 규칙
- [x] 정산 상태 흐름
- [x] DB 테이블/칼럼 매핑 (8개 테이블 확인 완료)

---

### 6️⃣ **QuotationAgent** (견적 담당자)
**책임:** 기업/단체 요청 처리 및 자동 견적

| 항목 | 내용 |
|------|------|
| **입력** | 기업 요청 (인원, 출발지, 목적지, 시간) |
| **의사결정** | 가격 제시 |
| **산출물** | 확정 견적 → 경로 생성 |
| **의존성** | RouteManagerAgent와 협력 |
| **실행주기** | 요청 시 (비정기) |

---

### 7️⃣ **DriverAgent** (기사)
**책임:** 배차 경로 운행 및 앱 데이터 입력

| 항목 | 내용 |
|------|------|
| **입력** | 배차된 경로 |
| **의사결정** | 운행 실행 (자동이 아님) |
| **산출물** | 운행 완료 + 앱 데이터 |
| **의존성** | DispatcherAgent의 배차 필수 |
| **실행주기** | 배차 시점에 실행 |

**주의:** 이것은 AI Agent가 아니라 실제 기사 → 앱 기반으로 운영

---

## 🔄 Agent 협력 흐름도

```
[신청자 데이터 들어옴]
    ↓
[RouteManagerAgent]
  - 클러스터링 분석
  - 경로 생성
    ↓
[DispatcherAgent]
  - 기사 배정
  - 배차 확정
    ↓
    ├─→ [MarketingAgent] (동시)
    │     - 신청자 모집
    │
    └─→ [DriverAgent]
          - 운행 실행
            ↓
          [ControlAgent]
            - 실시간 모니터링
              ↓
          [SettlementAgent] (월말)
            - 정산 계산
            - 보고서 생성
```

---

## 🛠️ 구현 계획

### Phase 1: Settlement Agent (우선순위 1)
**이유:** 월 1회 고정, 규칙 기반, 의사결정 단순

**정보 수집 현황:**
- [x] 계산 공식 (코드 분석 완료 2026-04-24)
- [x] 데이터 엔티티 구조
- [x] 특수 케이스 처리 규칙
- [x] DB 테이블 매핑 (8개 테이블 스키마 확인 완료 2026-04-24)

**예상 개발 기간:** 1-2주

### Phase 2: RouteManagerAgent + DispatcherAgent
**의존성:** SettlementAgent 완료 후

**필요 정보:**
- [ ] 클러스터링 판단 기준
- [ ] 경로 생성 규칙
- [ ] 기사 선택 알고리즘

### Phase 3: 나머지 Agent들
**마케팅, 관제, 견적**

---

## 📊 기대 효과

| 항목 | 현재 | 자동화 후 |
|------|------|----------|
| **정산** | 월말 3-4시간 수동 작업 | 0시간 (자동) |
| **경로 생성** | 운영팀 의사결정 필요 | 자동 생성 |
| **배차** | 수동 선택 | 최적화 자동 배정 |
| **마케팅** | 수동 연락 | 자동 SMS |
| **오류율** | 매월 2-3건 | 거의 0 |
| **운영 비용** | 운영팀 3명 필요 | 1명 모니터링만 |

---

## 🚀 기술 스택 (예상)

```
Framework: CrewAI / LangChain
Language: Python
LLM: Claude API
Backend: FastAPI
Database: [기존 mshuttle DB + 마이그레이션]
Scheduler: APScheduler (월별/일별 자동 실행)
Monitoring: 대시보드 (React)
```

---

## 📝 다음 단계

Settlement Agent 계산 로직 파악 완료 (2026-04-24). 남은 작업:

1. **백엔드 DB 테이블 매핑** — 프론트 인터페이스 ↔ 실제 DB 테이블/칼럼 확인
2. **백엔드 API 분석** — 정산 데이터 조회/저장 API 엔드포인트 파악
3. **Agent 코드 구현** — Python + FastAPI로 계산 로직 자동화
4. **자동 스케줄러** — 매월 자동 실행 + 알림
5. **검증** — 기존 정산 결과와 Agent 계산 결과 대조
